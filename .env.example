export API_KEY_ALCHEMY="YOUR_ALCHEMY_KEY"
# Etherscan (để trống nếu chưa verify; hoặc chạy source script/env-optional.sh trước forge script)
export API_KEY_ETHERSCAN="YOUR_API_KEY"
export API_KEY_ETHERSCAN_BASE="YOUR_API_KEY"
export API_KEY_ETHERSCAN_UNICHAIN="YOUR_API_KEY"
export PRIVATE_KEY="PRIVATE_KEY_USED_FOR_BROADCASTING"

export DRY_RUN=false
export FOUNDRY_PROFILE="default"

# Tests: use RPC fork (Arbitrum) vs mock (no RPC)
# FORK=1       → fork Arbitrum, real USDC (needs ARBITRUM_RPC_URL, can hit rate limits)
# FORK=0 or unset → mock USDC, no RPC (default, fast, CI-friendly)
# export FORK=0
# export ARBITRUM_RPC_URL="https://evm-42161.keplr.app"